name: open edge for test
on:
   push:
    branches:
       - main
jobs:
  my-job:
    runs-on: self-hosted
    steps:
      - name: run a nexus bat file e
        run: psexec -i 2 C:\Users\Administrator\Desktop\nexus.bat
